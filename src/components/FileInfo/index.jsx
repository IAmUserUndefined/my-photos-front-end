import React from 'react';

const FileInfo = ( { name } ) => {
    return ( 
        <>
            <div>
                <strong>{name}</strong>
            </div>
        </>
     );
}
 
export default FileInfo;